import 'package:flutter/material.dart';

class MotheringMagazineScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Magazine'),
      ),
    );
  }
}
